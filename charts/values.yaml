project: ${PROJECT}
domainSuffix: ${DOMAINSUFFIX}
identifierNumber: ${IDENTIFIERNUMBER}
namespaceTag:
  authority: ${AUTHNAMESPACETAG}
  common: ${COMMONSPACETAG}

resourcePreset: default
# resourcePreset: low

argocd:
  appname: ${NAMESPACE}
  namespace: ${ARGOCDNAMESPACE}

cluster:
  address: https://kubernetes.default.svc
  namespace: ${NAMESPACE}
  commonToolsNamespace: ${COMMONNAMESPACE}
  issuer: ${CLUSTERISSUER}

secrets:
  role: ${SECRETROLE}
  secretEngine: ${SECRETENGINE}

authority_iaa:
  enabled: true
  repoURL: "https://github.com/ForumViriumHelsinki/TFDS_SIMPL-open_authority-iaa.git"
  targetRevision: "fix/vault-webhook-injection-v3.1.0"
  path: "charts"
  valuesBranch: "fix/vault-webhook-injection-v3.1.0"

authority_gaia_x_edc:
  enabled: true
  repoURL: "https://github.com/ForumViriumHelsinki/TFDS_SIMPL-open_authority_gaia_x_edc.git"
  targetRevision: "fix/vault-webhook-injection-v3.1.0"
  path: "charts"
  valuesBranch: "fix/vault-webhook-injection-v3.1.0"

authority_monitoring:
  enabled: true
  projectID: "1394"
  targetRevision: 0.0.6
  chart_name: authority-monitoring
  valuesBranch: v0.0.6
